# Tracking IDs (defaults from Autoflowr Studio — override per business if you have your own)
NEXT_PUBLIC_GA4_ID={{ga4Id}}
NEXT_PUBLIC_META_PIXEL_ID={{pixelId}}
NEXT_PUBLIC_CLARITY_ID={{clarityId}}
NEXT_PUBLIC_BUSINESS_SLUG={{slug}}

# Lead endpoint (proxied through /api/lead)
# LEAD_UPSTREAM_URL={{leadEndpoint}}

# Origin allow-list (production host + any preview/staging hosts, comma-separated, no protocol)
ALLOWED_ORIGINS={{siteHost}}

# Telegram blog automation (Phase 10) — required only if blog enabled
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# ANTHROPIC_API_KEY=
# CRON_SECRET=

# Admin (used when admin endpoints are scaffolded)
# ADMIN_TOKEN=
# ADMIN_IPS=
