"use client";
import { motion } from "framer-motion";
import Image from "next/image";
import { copy } from "@/app/(content)/copy";

export default function Hero() {
  return (
    <section className="relative px-6 pt-24 pb-16 max-w-5xl mx-auto">
      <motion.div
        initial={{ opacity: 0, y: 16 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.6, ease: "easeOut" }}
      >
        <h1 className="text-4xl md:text-6xl font-extrabold leading-tight tracking-tight">
          {copy.hero.headline}
        </h1>
        <p className="mt-5 text-lg md:text-xl text-text/90 max-w-2xl">
          {copy.hero.subtitle}
        </p>
        <div className="mt-8 flex flex-wrap gap-3">
          <a
            href="#lead"
            className="inline-flex items-center justify-center rounded-full bg-accent text-black font-bold px-7 py-3 shadow-glow hover:scale-[1.02] transition-transform"
          >
            {copy.hero.primaryCta}
          </a>
          <a
            href={`https://wa.me/{{whatsappNumber}}?text=${encodeURIComponent(copy.hero.whatsappMessage)}`}
            target="_blank"
            rel="noopener"
            className="inline-flex items-center justify-center rounded-full border border-text/30 text-text font-semibold px-7 py-3 hover:border-accent hover:text-accent transition-colors"
          >
            {copy.hero.secondaryCta}
          </a>
        </div>
      </motion.div>

      <motion.div
        initial={{ opacity: 0, scale: 0.95 }}
        animate={{ opacity: 1, scale: 1 }}
        transition={{ duration: 0.8, delay: 0.15, ease: "easeOut" }}
        className="relative mt-12 aspect-[16/9] rounded-2xl overflow-hidden border border-text/10 shadow-glow"
      >
        <Image
          src="/images/hero.png"
          alt={copy.hero.imageAlt}
          fill
          priority
          sizes="(max-width: 768px) 100vw, 1024px"
          className="object-cover"
        />
      </motion.div>
    </section>
  );
}
